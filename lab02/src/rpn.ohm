ReversePolishNotation {
  Exp
    = Exp Exp "+"                                   -- add
    | Exp Exp "*"                                   -- mul
    | number                                        -- lit
  number = digit+     
}